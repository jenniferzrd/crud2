var app = (function app() {
    "use strict";

    var ui;

    window.onload = function start() {

        if (byId("tabler_user")) {
            ui = new UI();
            // log(ui);
        }

    };


}());
